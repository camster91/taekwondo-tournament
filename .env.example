# Database Configuration (provided by Coolify from linked Postgres service)
# Format: postgresql://USER:***@HOST:PORT/DBNAME
DATABASE_URL=postgresql://taekwondo:***@taekwondo-db:5432/taekwondo_tournament

# Server port. Dev default: 3001 (src/server/index.ts: `PORT || 3001`).
# Production (Vercel / Dockerfile): 3000. The Vite dev proxy maps /api -> localhost:3001.
PORT=3001

# JWT Secret (generate a strong random string for production)
# Example: openssl rand -base64 48
JWT_SECRET=your-j...e-me

# Admin Setup Key (optional)
ADMIN_SETUP_KEY=

# Allowed Origins (CORS) - comma-separated
# Example: https://tournament.yourdomain.com,https://tournament-staging.yourdomain.com
ALLOWED_ORIGINS=
